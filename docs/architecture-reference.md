<!--
Source: /Users/rafalaffeier/projects/asistente/documentos/AI_Assistant_Architecture_Professiona.docx
Generated by scripts/docx_to_markdown.py. Edit this Markdown as the working architecture reference.
The Word document remains the human-friendly formatted copy.
-->

| TECHNICAL ARCHITECTURE DOCUMENT AI Virtual Assistant Desktop · Mobile · Centralized Backend |
| --- |

| Status | Implementation-Ready |
| --- | --- |
| Version | 3.0 |
| Date | April 2026 |
| Scope | Sections 1 – 70 |
| Classification | Confidential |

This document describes the full technical architecture for the AI Virtual Assistant system, including backend design, agent communication, tool system, security model, scalability strategy, and the travel search & booking module.

# Table of Contents

# 1. System Overview

A multi-device AI assistant composed of three main layers:

Central Backend (control plane)

Desktop Agent (execution node with OS access)

Mobile App (remote interface)

The system interprets natural language and executes real-world actions across devices and integrations.

# 2. Objectives & Scope

## Objectives

Unified assistant across desktop and mobile

Natural language to structured actions

Persistent memory per user/workspace

Real OS-level execution

Multi-user scalability

## MVP Scope

Auth (OAuth + JWT)

Workspace isolation

Desktop agent + WebSocket

Action system (tools)

Confirmation flow

Google Calendar integration

Audit logging

# 3. System Architecture

The diagram below shows the complete system architecture, including all components, data flows, and integration points.

# 4. Desktop Agent Architecture

## Runtime

Agent Process

├── WebSocket Client

├── Command Dispatcher

├── Tool Executors

├── Permission Manager

└── Secure Storage (Keychain)

## Lifecycle

Install app

Generate device_id

Register device

Store agent_token

Open WebSocket

Listen for commands

# 5. Device Registration Flow

POST /devices/register

## Request

{

"device_name": "Rafa-MacBook",

"platform":    "macos",

"capabilities": ["filesystem", "terminal"]

}

## Response

{

"device_id":   "uuid",

"agent_token": "secure_token"

}

# 6. Communication Flows

## User to Execution

## Backend to Agent

{ "action_id": "...", "tool": "...", "params": {...} }

# 7. Workspace & Context Separation

All data scoped by workspace_id

No cross-workspace memory

Context = messages + memory + tools per workspace

# 8. Action / Tool System

## Tool Interface

class Tool:

name: str

version: str

scope: str

permission: str

requires_confirmation: bool

async def execute(params, context):

pass

# 9. Action Confirmation Flow

pending_confirmation

## Rules

TTL: 2–5 minutes

Single use

Stored hashed

# 10. Permissions Matrix

| Tool | Scope | Permission |
| --- | --- | --- |
| open_file | desktop | filesystem.read |
| delete_file | desktop | filesystem.write |
| run_command | desktop | terminal.exec |
| create_event | backend | calendar.write |

Enforced at two levels:

Backend (logical)

Agent (physical)

# 11. Local Command Execution Policy

Execution allowed only if: permission granted AND confirmed AND matches policy

## Command Policies

policy_type: allowlist | denylist

pattern: text

## Blocked by Default

rm -rf

sudo

chmod -R

curl | sh

# 12. Offline Queue Behavior

## Flow

action created

## Rules

TTL: 24 hours

Retry: max 3 attempts

Confirmation revalidated on reconnect

Expired actions are rejected

# 13. AI Layer Contract

{ "intent": "...", "tool": "...", "params": {...} }

## Validation

Schema enforced on every response

Unknown tools rejected immediately

# 14. Database Schema

### users

| id uuid |
| --- |
| email |
| password_hash |
| created_at |

### workspaces

| id uuid |
| --- |
| name |
| owner_user_id |
| created_at |

### workspace_users

| workspace_id |
| --- |
| user_id |
| role |

### devices

| id |
| --- |
| user_id |
| workspace_id |
| device_name |
| platform |
| agent_token_hash |
| last_seen_at |

### permissions

| user_id |
| --- |
| device_id |
| permission_key |
| granted |

### tools

| name |
| --- |
| version |
| scope |
| permission_key |
| requires_confirmation |
| schema jsonb |

### actions

| id |
| --- |
| workspace_id |
| user_id |
| device_id |
| tool_name |
| tool_version |
| params jsonb |
| status |
| requires_confirmation |
| confirmed_at |
| retry_count |
| result jsonb |
| created_at |

### confirmation_tokens

| action_id |
| --- |
| token_hash |
| expires_at |
| used_at |

### messages

| id |
| --- |
| workspace_id |
| user_id |
| thread_id |
| input_text |
| ai_output jsonb |
| created_at |

### memory

| workspace_id |
| --- |
| user_id |
| key |
| value jsonb |
| scope |

### integrations

| user_id |
| --- |
| provider |
| access_token |
| refresh_token |
| expires_at |

### audit_logs

| workspace_id |
| --- |
| user_id |
| device_id |
| event_type |
| payload jsonb |
| created_at |

# 15. Audit Logs

user.login

device.registered

action.requested

action.confirmed

action.executed

action.failed

integration.connected

Append-only. No updates allowed.

# 16. Security Model

JWT auth with token rotation

OS keychain storage for secrets

Device revocation support

Signed WebSocket connections

## Sensitive Actions Require

Explicit user confirmation

Full audit trail

# 17. Scalability Strategy

Stateless backend design

Redis Pub/Sub for WebSocket routing

Celery for async task processing

DB indexing + JSONB for flexible data

AI response caching

# 18. Technical Risks & Mitigation

| Risk | Mitigation |
| --- | --- |
| Agent offline | Queue + retry with 24h TTL and max 3 attempts |
| LLM errors | Strict JSON schema validation + confirmation flow |
| Token leak | Short TTL + immediate revocation capability |
| OS abuse | Permission matrix + command policy enforcement |

# 19. MVP Acceptance Criteria

User login + workspace selection

Desktop registers and connects

WebSocket active

NL input converts to structured action

Backend tool works (calendar)

Desktop tool works (open file)

Confirmation required for sensitive actions

Token validation enforced

Tool version stored

Offline actions queued and executed on reconnect

Audit logs populated

Mobile syncs with desktop actions

# 20. Recommended First Implementation Steps

FastAPI base setup

Auth + workspaces

Devices registration

WebSocket server

Minimal agent (connect + ping)

Tool registry

One desktop tool: open_file

One backend tool: Google Calendar

Confirmation flow

Audit logging

# 21. Execution Engine for Multi-Step Actions

The system must support complex user requests that require more than one tool execution.

User input

A single user request may generate one parent action and multiple ordered action_steps.

# 22. action_steps Table

### action_steps

| id uuid |
| --- |
| action_id uuid |
| step_order int |
| tool_name varchar |
| tool_version varchar |
| params jsonb |
| status varchar -- pending \| running \| done \| failed \| skipped |
| result jsonb nullable |
| error text nullable |
| created_at timestamp |
| updated_at timestamp |

## Rules

Steps execute in order unless explicitly marked as parallelizable.

A failed required step stops the parent action.

Optional failed steps may continue if the execution plan allows it.

Each step must store its own result and error state.

# 23. Idempotency for Actions

Protection against duplicate execution caused by retries, network errors, or repeated user requests.

idempotency_key  varchar  unique

input_hash       varchar

## Rules

Same user + workspace + idempotency_key must not create duplicate actions.

Repeated requests with the same key return the existing action state.

input_hash is generated from normalized user input + resolved tool + params.

# 24. Desktop Agent Heartbeat

status        varchar  -- online | offline | busy

last_ping_at  timestamp

## Rules

Agent sends ping every 15–30 seconds.

Backend marks device offline if no ping within timeout.

Busy status is set while executing a local action.

# 25. Device Trust Level

| trust_level | Capabilities |
| --- | --- |
| low | Read-only actions only |
| standard | Normal approved actions |
| trusted | Advanced local actions — still subject to confirmation and policy |

Critical actions require a trusted device.

# 26. Device Revocation Endpoint

POST /devices/{id}/revoke

Invalidate agent token.

Close active WebSocket session.

Reject pending actions for that device unless reassigned.

Write audit log event: device.revoked.

# 27. Resource Locks

### resource_locks

| id uuid |
| --- |
| workspace_id uuid |
| resource_type varchar -- file \| calendar_event \| integration \| device \| custom |
| resource_id varchar |
| locked_by_action_id uuid |
| expires_at timestamp |
| created_at timestamp |

## Rules

Locks must have expiration.

Actions must acquire lock before modifying a protected resource.

If lock exists, action waits, fails, or queues depending on tool policy.

# 28. Action Timeout

actions:      timeout_seconds  int

action_steps: timeout_seconds  int  nullable

## Rules

If timeout is reached, action status becomes failed.

Long-running actions must be delegated to the background job system.

# 29. Retry Policy per Tool

retryable              bool

max_retries            int

retry_backoff_seconds  int

## Rules

Read operations are usually retryable.

Write operations are retryable only when idempotency is guaranteed.

Desktop destructive actions are not retryable by default.

# 30. Risk Level Model

| risk_level | Confirmation Rule |
| --- | --- |
| low | No confirmation required |
| medium | Optional — depends on workspace policy |
| high | Confirmation required |
| critical | Confirmation + explicit text confirmation |

# 31. Confirmation Payload Visible to User

{

"summary":    "Create calendar event",

"details": { "title":"Meeting with Juan","date":"2026-05-01","time":"10:00" },

"risk_level": "medium"

}

## Rules

Do not execute actions based on hidden params.

The approved payload must match the final execution payload.

If params change, a new confirmation token is required.

# 32. Tool Sandboxing

## Sandbox Rules

Restricted working directory

Clean environment variables

No root privileges

No unrestricted shell access

Allowed paths only

Allowlist-first command execution

{

"cwd":           "/Users/rafa/Workspace",

"allowed_paths": ["/Users/rafa/Workspace", "/Users/rafa/Downloads"],

"env_policy":    "clean",

"shell":         false

}

# 33. Command Execution Logs

### command_logs

| id uuid |
| --- |
| action_id uuid |
| device_id uuid |
| workspace_id uuid |
| command text |
| exit_code int |
| stdout text nullable |
| stderr text nullable |
| duration_ms int |
| created_at timestamp |

## Rules

Logs must not expose secrets.

stdout and stderr may be truncated.

Sensitive command output should be redacted before storage.

# 34. AI Confidence Score

{ "intent":"create_event","tool":"create_event","confidence":0.87,"params":{...} }

## Rules

High-risk actions may require a higher confidence threshold

# 35. AI Fallback for Invalid JSON

Retry once with a repair prompt.

Validate again.

If still invalid, fail safely.

Ask the user for clarification if needed.

## Rules

Never execute actions from invalid or partially parsed AI output.

All failed AI interpretations must be logged.

# 36. Tool Schema Validation

## Rules

Missing required params reject the action.

Unknown params are rejected unless the tool explicitly allows additional properties.

# 37. AI Usage and Cost Logging

### ai_usage_logs

| id uuid |
| --- |
| user_id uuid |
| workspace_id uuid |
| model varchar |
| input_tokens int |
| output_tokens int |
| estimated_cost numeric |
| request_type varchar |
| created_at timestamp |

## Rules

Every AI call must create a usage log.

Usage must be queryable per user, workspace, and date range.

# 38. Rate Limiting and Quotas

## Rate Limits Applied Per

User

Workspace

Endpoint

AI model

## Quota Examples

monthly_ai_token_limit

monthly_action_limit

max_daily_desktop_commands

## Rules

Exceeded quota blocks new actions and returns a structured error.

Admins may configure workspace-level limits.

# 39. Separation of Intent, Decision, and Execution

| Layer | Table |
| --- | --- |
| What the user asked | user_intents |
| What the AI/system decided | system_decisions |
| What was actually executed | actions |

### user_intents

| id uuid |
| --- |
| workspace_id uuid |
| user_id uuid |
| thread_id uuid |
| raw_input text |
| normalized_input text |
| created_at timestamp |

### system_decisions

| id uuid |
| --- |
| intent_id uuid |
| selected_tool varchar |
| selected_tool_version varchar |
| confidence numeric |
| reasoning_summary text nullable |
| params jsonb |
| created_at timestamp |

# 40. Thread Model

### threads

| id uuid |
| --- |
| workspace_id uuid |
| user_id uuid |
| title varchar |
| created_at timestamp |
| updated_at timestamp |
| archived_at timestamp nullable |

## Rules

Messages belong to a thread.

Context is loaded from the active thread only unless explicitly requested.

Workspace memory may be shared across threads.

# 41. Memory Classification

type                varchar  -- preference | fact | credential_reference | project_context

confidence          numeric

expires_at          timestamp  nullable

data_classification varchar  -- public | internal | sensitive | secret

## Rules

Do not store raw credentials, tokens, or private keys in normal memory.

Sensitive memories require explicit user permission.

Expired memories are not loaded into AI context.

# 42. Integration Token Encryption

## Rules

Use KMS, Vault, or application-level envelope encryption.

Never store OAuth tokens in plaintext.

Refresh tokens require stronger protection than access tokens.

status             varchar  -- connected | expired | revoked | error

last_refreshed_at  timestamp  nullable

# 43. Refresh Token Rotation

Refresh tokens are updated when provider returns a new one.

Failed refresh marks integration as expired or error.

User must reconnect when refresh is no longer valid.

# 44. Background Job System

## Recommended Stack

Celery + Redis

RQ + Redis

Arq + Redis

### jobs

| id uuid |
| --- |
| action_id uuid |
| status varchar -- queued \| running \| done \| failed |
| started_at timestamp nullable |
| finished_at timestamp nullable |
| error text nullable |
| created_at timestamp |

## Rules

Jobs must be linked to actions.

Action status must reflect job status.

# 45. Notification System

### notifications

| id uuid |
| --- |
| user_id uuid |
| workspace_id uuid |
| type varchar |
| title varchar |
| body text |
| read_at timestamp nullable |
| created_at timestamp |

## Notification Channels

Desktop in-app

Mobile push

Email (optional, later phases)

# 46. Real-Time Event Bus

## Events Emitted

action.status_changed

device.online

device.offline

confirmation.required

integration.expired

job.failed

## Rules

Clients subscribe through WebSocket.

Backend services publish/consume through Redis Pub/Sub or message broker.

# 47. API Versioning

/api/v1/auth

/api/v1/workspaces

/api/v1/devices

/api/v1/actions

/api/v1/tools

## Rules

Breaking changes require a new API version.

Clients declare compatible API version.

# 48. Tool Registry Administration

### workspace_tools

| id uuid |
| --- |
| workspace_id uuid |
| tool_name varchar |
| tool_version varchar |
| enabled bool |
| created_at timestamp |

## Rules

A disabled tool cannot be selected by AI.

Workspace admins may enable/disable tools.

Dangerous tools are disabled by default.

# 49. Workspace-Level Permissions and RBAC

| Role | Capabilities |
| --- | --- |
| owner | Can manage workspace and devices |
| admin | Can manage tools and integrations |
| member | Can execute allowed actions |
| viewer | Read only |

### workspace_permissions

| workspace_id uuid |
| --- |
| role varchar |
| permission_key varchar |
| granted bool |

# 50. Immutable Audit Chain

hash_previous  varchar  nullable

hash_current   varchar

## Rules

Each audit row hashes its payload and previous hash.

Tampering can be detected by verifying the chain.

# 51. Data Privacy and Classification

| Classification | Description |
| --- | --- |
| public | No restrictions |
| internal | Internal use only, not for external sharing |
| sensitive | Must be minimized in logs; explicit permission required |
| secret | Never sent to AI prompts; highest protection level |

## Rules

Secrets must never be sent to AI prompts.

Sensitive data should be minimized in logs.

User data must be exportable and deletable.

# 52. Data Retention Policy

| Data Type | Retention Period |
| --- | --- |
| messages | Configurable, default 12 months |
| audit_logs | Minimum 12 months |
| action logs | 12 months |
| command logs | 30–90 days depending on sensitivity |
| AI usage logs | 24 months for billing/debugging |

## Rules

Retention must be workspace configurable where possible.

Deletion jobs must run periodically.

# 53. GDPR Export and Delete

GET    /users/me/export

DELETE /users/me

## Rules

Export includes messages, actions, memory, devices, integrations metadata, and audit references.

Delete removes or anonymizes personal data depending on legal/audit requirements.

Integration tokens must be revoked where possible.

# 54. Secrets Management

## Allowed Solutions

Docker secrets

Vault

Cloud Secrets Manager

KMS-backed encrypted config

## Rules

Secrets must not be logged.

Secrets must not be included in AI context.

Secrets rotation must be supported.

# 55. Structured Error Model

{

"error_code": "ACTION_PERMISSION_DENIED",

"message":    "The user does not have permission to execute this action.",

"details":    {}

}

## Rules

Never return raw internal exceptions to clients.

Errors must be traceable through request_id.

# 56. Observability

## Required

Structured logs

Metrics

Tracing

Request IDs

Action IDs in logs

Device IDs in agent logs

GET /health

GET /ready

GET /metrics

## Rules

/health confirms process is alive.

/ready confirms dependencies are reachable.

/metrics exposes Prometheus-compatible metrics.

# 57. Backup Strategy

Daily backups minimum.

Restore must be tested regularly.

Backups must be encrypted.

Production restore process must be documented.

# 58. Database Migration Strategy

Use Alembic for database migrations.

Every schema change requires a migration.

Migrations must be reversible where possible.

Production migrations must be tested in staging first.

# 59. Testing Strategy

## Required Test Levels

Unit tests

Integration tests

Agent tests

End-to-end tests

Security tests for permissions and confirmations

## Critical Scenarios

AI creates valid action

Invalid AI JSON is rejected

Sensitive action requires confirmation

Expired token cannot execute

Revoked device cannot connect

Offline action queues and executes on reconnect

Command policy blocks unsafe commands

Workspace context does not leak

# 60. Environment Separation

| Environment | Notes |
| --- | --- |
| local | Developer machines — uses mocked secrets |
| dev | Shared development — real integrations, test data |
| staging | Pre-production — mirrors production config |
| production | Live system — full secrets, real OAuth credentials |

Production data must not be used in local/dev without anonymization.

# 61. Feature Flags

Risky features must be controllable through feature flags without requiring a code deployment.

enable_multi_step_actions

enable_desktop_terminal_tool

enable_ai_memory

enable_offline_queue

enable_audit_chain_hashing

# 62. Travel Search & Booking Module

The assistant can search flights, hotels and travel options using external travel APIs instead of scraping websites.

## Main Providers to Evaluate

| Provider | Capabilities |
| --- | --- |
| Amadeus API | Flights, hotels, booking, airport data |
| Skyscanner API | Flight metasearch and price comparison |
| Kiwi / Tequila API | Flexible routes and multi-city searches |
| Expedia Rapid API | Strong hotel inventory |
| Travelpayouts API | Affiliate links, price trends and monetization |

## Recommended MVP Approach

Use API search + affiliate/redirect booking links.

Do not process payments or issue tickets inside the MVP.

## Request Flow

User request

## Travel Tools

| Tool | Description |
| --- | --- |
| search_flights | Query flight availability and pricing across providers |
| search_hotels | Query hotel inventory by location, dates and preferences |
| search_trip_package | Combined flight + hotel package search |
| compare_prices | Cross-provider price comparison for same route/dates |
| track_price_drop | Register a price alert for a given search |

## Database Additions

### travel_searches

| id uuid |
| --- |
| workspace_id uuid |
| user_id uuid |
| origin varchar |
| destination varchar |
| departure_date date |
| return_date date nullable |
| passengers int |
| budget numeric nullable |
| preferences jsonb |
| created_at timestamp |

### travel_results

| id uuid |
| --- |
| travel_search_id uuid |
| provider varchar |
| result_type varchar -- flight \| hotel \| package |
| price numeric |
| currency varchar |
| score numeric |
| raw_payload jsonb |
| booking_url text |
| created_at timestamp |

### travel_price_alerts

| id uuid |
| --- |
| workspace_id uuid |
| user_id uuid |
| travel_search_id uuid |
| target_price numeric |
| status varchar -- active \| triggered \| expired |
| created_at timestamp |

## Integration Notes

Travel tools registered in tool registry with scope: backend and risk_level: low.

API keys must be stored via the secrets management system — never in plain config.

Raw API responses in raw_payload are normalized before being returned to the AI.

Ranking engine scores results by price, duration, stops, and user preferences.

Price alerts are processed by a background job that re-runs the search periodically.

Booking URLs must be validated — affiliate links checked against an allowlist.

# 63. Travel Booking Policy

The system does not handle payments, ticket issuing, or reservations directly. All booking actions must redirect the user to the provider's official platform.

# 64. Booking Flow

User request

# 65. Travel Tool Contract

## Example: search_flights

{

"tool": "search_flights",

"params": {

"origin":         "MAD",

"destination":    "NYC",

"departure_date": "2026-06-10",

"return_date":    "2026-06-20",

"passengers":     1,

"budget":         800,

"preferences":    { "nonstop": true, "cabin": "economy" }

}

}

## Normalized Response

{

"results": [{

"provider":       "amadeus",

"price":          520,

"currency":       "EUR",

"departure_time": "2026-06-10T08:30:00",

"arrival_time":   "2026-06-10T14:10:00",

"score":          0.92,

"booking_url":    "https://provider.com/booking/xyz"

}]

}

# 66. Booking Rules

The system must never auto-purchase tickets.

The system must not store payment data.

The system must always require user interaction before redirect.

Display key information before redirect: price, provider, dates, route, and conditions.

Booking URLs must enforce HTTPS and match allowed provider domains.

Only whitelisted travel providers may be used for booking redirects.

# 67. UI Behavior Before Redirect

# 68. Tracking and Attribution

Where supported by the provider, booking_url may include affiliate or tracking parameters.

This allows click tracking, conversion metrics, and optional monetization without processing payments inside the system.

# 69. Security Considerations for Booking Redirects

Validate booking_url before returning it to the user.

Allow only HTTPS URLs.

Match the final URL domain against the provider allowlist.

Reject shortened, masked, or unknown redirect URLs unless explicitly approved.

Log every booking redirect in audit_logs.

Never include OAuth tokens, API keys, or internal identifiers in booking URLs.

# 70. MVP Boundary for Travel Booking

The following features are explicitly out of scope for the MVP:

Payment processing

Ticket issuing

Refund handling

Booking modifications

Customer support for travel providers

Storage of payment cards or billing credentials

Focus the MVP on search, comparison, and redirect. Booking features can be added in future phases once the API integrations are stable and tested.

## 71. Voice Interface

The system supports voice interaction as an alternative input and output layer.

Voice functionality allows users to communicate with the assistant using spoken language instead of text, while preserving the same internal processing pipeline.

Voice operates as an additional interface and does not modify the core architecture of the system.

Capabilities:

Near real-time interaction (depending on mode)

## 72. Voice Flow

Rules:

Voice input must be converted to text before entering the AI pipeline.

Voice output must be generated only after a validated response is produced.

Voice must not bypass validation, confirmation, or permission checks.

## 73. Voice Integration (OpenAI)

Speech-to-Text:

OpenAI Whisper API (recommended for MVP)

Optional: OpenAI Realtime API for streaming (future phase)

Text-to-Speech:

OpenAI TTS models for audio generation

Processing Model:

Audio input is sent to STT service.

Transcribed text is injected into the existing AI flow.

Text response is converted to audio via TTS.

Audio is returned to the client for playback.

Rules:

Voice processing must be stateless.

Audio must not be coupled to business logic execution.

AI output validation rules apply equally to voice and text inputs.

## 74. Voice Modes

Mode 1: Push-to-Talk (MVP)

User presses a button to record audio.

Audio is captured and sent after recording stops.

Response is returned after full processing.

Mode 2: Streaming / Real-Time (Future)

Continuous audio input.

Partial transcription and incremental responses.

Lower latency but higher infrastructure complexity.

Rules:

Push-to-talk is the only mode required for MVP.

Streaming mode must not be enabled without proper cost and latency control.

## 75. Voice Security & Privacy

Microphone access must require explicit user permission.

Audio must not be stored by default.

Transcriptions must follow the same privacy rules as text messages.

Sensitive conversations must not be stored as raw audio.

Rules:

No audio data may be stored without explicit user consent.

Audio must not be included in logs.

Sensitive content must be handled according to data classification rules.

## 76. Voice Cost Control

Voice features introduce additional operational cost due to STT and TTS usage.

Control Mechanisms:

Limit maximum audio duration per request.

Apply rate limiting per user and workspace.

Track usage via AI usage logs.

Disable streaming mode in MVP.

Rules:

All STT and TTS calls must be logged in ai_usage_logs.

Exceeding usage limits must block further voice interactions.

## 77. Voice Data Model (Optional)

voice_interactions

id uuid
 workspace_id uuid
 user_id uuid
 message_id uuid
 duration_seconds int
 stt_cost numeric
 tts_cost numeric
 created_at timestamp

Rules:

This table is optional and used for analytics and cost tracking.

Audio content must not be stored unless explicitly required and approved.

## 78. Voice MVP Scope

Included in MVP:

Push-to-talk interaction

Speech-to-Text (STT)

Text-to-Speech (TTS)

Basic voice response playback

Excluded from MVP:

Continuous streaming voice

Autonomous voice-driven agents

Long-term audio storage

Voice-based authentication

Rules:

Voice must remain an interface layer only.

All core logic, validation, and execution must follow the same rules as text-based interactions.

# 79. Local-First File Memory Architecture

The assistant memory should be designed as a local-first, file-based system. Instead of treating the relational database as the source of truth for long-term user memory, the primary memory record should be stored as human-readable Markdown files. This keeps the memory portable, inspectable, easy for the AI to load selectively, and independent from any single database engine.

The desktop application should maintain a local memory folder, and the server should synchronize encrypted copies of those files so the user can access their assistant memory from desktop, mobile, and future web clients.

## Memory Folder Layout

| Path | Purpose |
| --- | --- |
| memory/journal/YYYY/MM/YYYY-MM-DD.md | Daily journal with tasks, decisions, events, and pending items. |
| memory/summaries/YYYY-Www.md | Weekly summaries generated from daily journals. |
| memory/summaries/YYYY-MM.md | Monthly summaries for fast high-level recall. |
| memory/entities/projects.md | Known projects, current status, owners, and context. |
| memory/entities/people.md | User-approved people/entity memory. |
| memory/entities/preferences.md | Stable user preferences and assistant behavior notes. |
| memory/index/* | Rebuildable local search indexes and caches. Not the source of truth. |

## Daily Journal Contract

Each day should be represented by one Markdown file. The assistant writes concise, structured entries rather than unbounded raw logs.

- Facts: important events that happened during the day.

- Tasks: work completed or started by the user or assistant.

- Decisions: architectural, product, technical, or personal decisions.

- Pending: follow-up items that should remain visible.

- Technical events: errors, deployments, environment changes, and relevant system state.

# 80. Server-Synchronized Markdown Memory

The server is still required for a functional multi-device product. However, its role should be to synchronize and authorize access to encrypted memory files, not to become the main long-term memory database.

| Component | Responsibility |
| --- | --- |
| Desktop App | Maintains the full local memory folder and decrypts memory for local AI use. |
| Mobile App | Reads synchronized memory, edits notes, and sends remote actions through the backend. |
| Backend API | Handles auth, devices, permissions, sync metadata, and relay between clients. |
| Object Storage | Stores encrypted Markdown files and attachments. |
| PostgreSQL | Stores administrative metadata only: users, devices, sessions, versions, permissions, and sync state. |

## Sync Rules

- Markdown files are the source of truth for assistant memory.

- Search indexes, embeddings, and local SQLite caches are rebuildable.

- Each file has a version, checksum, modified timestamp, and owning workspace.

- Clients upload encrypted file changes and download encrypted updates.

- Conflicts should be resolved per file, preserving both versions when automatic merge is unsafe.

# 81. Minimal Database Role

A relational database remains useful, but it should be intentionally small. It should support the product infrastructure rather than store the user's private memory content.

| Database Area | Recommended Use |
| --- | --- |
| users | Account identity, login provider, and account status. |
| devices | Registered desktop/mobile clients, trust level, revocation state. |
| sessions | Short-lived access sessions and refresh token rotation metadata. |
| file_versions | Path, owner, version, checksum, size, and encrypted storage reference. |
| sync_events | Append-only metadata about uploads, downloads, merges, and conflicts. |
| permissions | Workspace and device-level access rules. |
| billing | Optional future subscription and plan metadata. |

The database must not be treated as the assistant's narrative memory. If an index or embedding store is added later, it should be considered derived data that can be deleted and rebuilt from the Markdown source files.

# 82. End-to-End Encryption for Memory Files

Private memory files should be encrypted before upload. The server and object storage should persist encrypted bytes and minimal metadata. Plaintext should exist only on authorized user devices during active use.

| Security Control | Requirement |
| --- | --- |
| Client-side encryption | Encrypt Markdown files before upload; decrypt only on authorized devices. |
| Per-user keys | Use keys scoped to a user or workspace, never a single global application key. |
| Device key storage | Store device secrets in macOS Keychain, Windows DPAPI/Credential Manager, iOS Keychain, or Android Keystore. |
| Signed URLs | Use short-lived signed URLs for object storage access; never public memory URLs. |
| Transport security | Require HTTPS for all API and storage operations. |
| Metadata minimization | Avoid putting sensitive content in filenames, tags, logs, or database fields. |
| Audit events | Log access metadata without storing decrypted memory content. |

# 83. Recovery, Revocation, and Privacy Trade-Offs

End-to-end encryption creates an explicit recovery trade-off. If the service cannot read user memory, it also cannot recover that memory without a user-held recovery method. The product must make this clear and provide safe recovery flows.

- Support device revocation so a lost laptop or phone can no longer sync memory.

- Provide a recovery kit or recovery phrase for restoring encrypted memory on a new device.

- Make it clear that losing all authorized devices and recovery material may make encrypted memory unrecoverable.

- Allow users to opt into encrypted cloud backup while preserving local Markdown export.

- Keep the product usable offline by writing locally first and syncing when connectivity returns.

## Architecture Decision

The recommended architecture is: server required, database minimal, memory file-based, and private content encrypted end-to-end. This provides multi-device functionality without turning the user's long-term assistant memory into an opaque database.
